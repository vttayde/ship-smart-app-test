// Temporary stub for '@prisma/client' to keep build running.
// Replace with real generated client once installation works and run `npx prisma generate`.
export class PrismaClient {
  user = {
    findUnique: async (_args: any) => null,
  };
  // add other model namespaces if needed
}
